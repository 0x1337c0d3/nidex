{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Params for ACP `terminal/create` (server→client): asks the client to open a new terminal process and return a handle.",
  "properties": {
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "sessionId"
  ],
  "title": "AcpTerminalCreateParams",
  "type": "object"
}