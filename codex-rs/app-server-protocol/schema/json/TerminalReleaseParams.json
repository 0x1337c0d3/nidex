{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Params for ACP `terminal/release` (server→client): releases the terminal handle without killing the process.",
  "properties": {
    "terminalId": {
      "type": "string"
    }
  },
  "required": [
    "terminalId"
  ],
  "title": "AcpTerminalReleaseParams",
  "type": "object"
}