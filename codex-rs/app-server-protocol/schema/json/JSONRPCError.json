{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "JSONRPCErrorError": {
      "properties": {
        "code": {
          "format": "int64",
          "type": "integer"
        },
        "data": true,
        "message": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    },
    "RequestId": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "format": "int64",
          "type": "integer"
        }
      ],
      "description": "A JSON-RPC request identifier — either a string or an integer.\n\n`#[serde(untagged)]` is intentionally NOT used here. Untagged enum deserialization inside serde's internally-tagged `ClientRequest` enum (which uses `#[serde(tag = \"method\")]`) triggers a known serde limitation: the error from the first failed variant attempt (\"invalid type: integer, expected a string\") leaks out instead of being silently retried. A manual `Visitor` with `deserialize_any` avoids this."
    }
  },
  "description": "A response to a request that indicates an error occurred.",
  "properties": {
    "error": {
      "$ref": "#/definitions/JSONRPCErrorError"
    },
    "id": {
      "$ref": "#/definitions/RequestId"
    }
  },
  "required": [
    "error",
    "id"
  ],
  "title": "JSONRPCError",
  "type": "object"
}