{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Response for the Zed ACP `session/prompt` method. Sent only after the full turn completes (long-polling style).",
  "properties": {
    "output": {
      "description": "Full text of the agent's response for this turn. Included here so clients (e.g. Zed) that don't implement streaming item notifications can still display the response.",
      "type": [
        "null",
        "string"
      ]
    },
    "stopReason": {
      "type": "string"
    }
  },
  "required": [
    "stopReason"
  ],
  "title": "SessionPromptResponse",
  "type": "object"
}